# Lav en lang tilfældig værdi, fx med: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=indsæt-en-lang-tilfældig-hemmelig-nøgle

# Sæt til true når appen kører bag HTTPS/Cloudflare.
SESSION_COOKIE_SECURE=false
